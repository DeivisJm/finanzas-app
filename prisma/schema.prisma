generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Gasto {
  id          Int      @id @default(autoincrement())
  descripcion String
  monto       Int
  createdAt   DateTime @default(now())
}